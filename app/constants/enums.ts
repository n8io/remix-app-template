enum App {
  CODE_NAME = "remix-starter",
  DESCRIPTION = "Welcome to remix!",
  NAME = "Remix Starter",
}

enum ContentType {
  HTML = "text/html",
}

enum Header {
  CONTENT_TYPE = "Content-Type",
  COOKIE = "Cookie",
  SET_COOKIE = "Set-Cookie",
}

export { App, ContentType, Header };
